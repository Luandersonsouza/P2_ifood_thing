# Testes de Aceitação - MyFood

Esta pasta contém os scripts de teste para o EasyAccept.

## Como executar

1. Compile o projeto (as classes estarão em `bin`).
2. Certifique-se de que o `EasyAccept.jar` está na `lib`.
3. Execute: